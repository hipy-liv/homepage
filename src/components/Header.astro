<header>
	<nav aria-label="Main navigation">
		<a class="brand" href="/" aria-label="HiPy home">
			<img src="/hipy-logo.png" alt="HiPy" width="170" height="280" />
		</a>
		<div class="nav-links">
			<a href="/#about">About</a>
			<a href="/#events">Events</a>
			<a href="/#resources">Resources</a>
		</div>
		<a class="nav-cta" href="/#events">Join the next session <span aria-hidden="true">↗</span></a>
	</nav>
</header>

<style>
	header {
		height: 86px;
		border-bottom: 1px solid var(--line);
		background: var(--cream);
		position: relative;
		z-index: 10;
	}

	nav {
		position: relative;
		width: min(1180px, calc(100% - 48px));
		height: 100%;
		margin: auto;
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	.brand {
		width: 60px;
		height: 70px;
		padding: 4px 9px;
		background: var(--ink);
		display: flex;
		align-items: center;
		justify-content: center;
		transform: rotate(-2deg);
		box-shadow: 4px 4px 0 var(--lime);
	}

	.brand img {
		display: block;
		width: 42px;
		height: 62px;
		object-fit: contain;
	}

	.nav-links {
		position: absolute;
		left: 50%;
		transform: translateX(-50%);
		display: flex;
		gap: 34px;
	}

	.nav-links a {
		text-decoration: none;
		font-size: 0.9rem;
		font-weight: 700;
	}

	.nav-links a:hover {
		text-decoration: underline;
		text-underline-offset: 5px;
	}

	.nav-cta {
		background: var(--coral);
		border: 2px solid var(--ink);
		padding: 9px 13px;
		text-decoration: none;
		font-size: 0.85rem;
		font-weight: 700;
		box-shadow: 3px 3px 0 var(--ink);
	}

	@media (max-width: 680px) {
		header { height: 72px; }
		nav { width: calc(100% - 28px); }
		.nav-links { display: none; }
		.nav-cta { font-size: 0.75rem; }
		.brand { width: 48px; height: 56px; padding: 4px 7px; }
		.brand img { width: 34px; height: 50px; }
	}
</style>
